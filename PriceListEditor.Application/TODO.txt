to-do list:

+styling views
+add classes to data model of application for mapping with visual diagram of databases
+make pagination for product list view
+create category selector for product list view
-add a few features to product list 
-add buttons with crud actions displaying on model popup on product list view
-make another list of pricelists of products (controller,view & buttons etc.) 
-try to make another in another list some features wiht items of lst without page reloading 

make at now
- add enum of sort orders
- change controller of products (ProductController cs in Controllers folder) to using sorting on price
- change view of product list (Product cshtml in Views/Product folder) to using sorting on price
- change page size selector view to using sorting on price
- change URL scheme to using sorting on price
- change page link tag helper to using sorting on price
- fix markup on product list page (in one line header and price sort order link)
- fix change sort order after page redirect (remove price sort order appointment in page link tag helper)
- fix not working link of price sorting 

make in future
- fix URL on first redirect to product list page (old url scheme is on all redirect links ???)
- make tag helper for price sort order
- fix old unit tests
- make unit tests for price sort order
- add sorting feature(see on metainit - link in DevNote txt) 
- try to add search feature(see on links in DevNote txt) 