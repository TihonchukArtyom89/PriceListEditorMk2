@model BaseListViewModel

<form method="post" id=@("form"+@Model.PageViewModel.Pseudonym+"PageSizeSelector") asp-controller="Product" asp-action="RedirectWithPageSizeSelected" asp-route-category="@ViewBag.SelectedCategory" asp-route-productPage="@ViewBag.SelectedPage" asp-route-sortOrder="@ViewBag.SortOrder">
    <div class="d-grip gap-2">
        <b class="m-1">Выберите размер страницы</b>
        <select id="pageSizeSelector" name="pageSize">
            @foreach (int pageSize in Model.PageSizes)
            {
                @if (pageSize == ViewBag.SelectedPageSize)
                {
                    <option selected>@pageSize</option>
                }
                else
                {
                    <option value="@pageSize">@pageSize</option>
                }
            }
        </select>
        <button id="submitButton" type="submit" class="btn btn-outline-secondary btn-sm">Выбрать</button>
    </div>
</form>